import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  app: {
    flex: 1,
  },
  scrollView: {
    minHeight: '100%',
  },
  btnPressed: {
    backgroundColor: 'white',
  },
});
